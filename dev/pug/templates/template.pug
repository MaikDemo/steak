<!DOCTYPE html>
html(lang="es")
    head
        meta(charset="UTF-8")
        meta(name="viewport", content="width=device-width, initial-scale=1.0")
        block config
            include ../config/config
        include ../includes/head
    body
        block content
        block preloading
            include ../includes/preloading
        block scripts
            script(src="app.js")